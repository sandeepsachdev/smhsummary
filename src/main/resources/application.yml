server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: smh-summary

anthropic:
  api-key: ${ANTHROPIC_API_KEY:}

resend:
  api-key: ${RESEND_API_KEY:}
  from: ${EMAIL_FROM:Briefing <onboarding@resend.dev>}
  to: ${EMAIL_TO:}

logging:
  level:
    root: INFO
    com.smhsummary: INFO
